spring:
  cloud:
    config:
      server:
        git:
          uri: ${SPRING_CLOUD_CONFIG_SERVER_GIT_URI:https://github.com/ATHARV-METKARI/RenStation-Config.git}
          default-label: main
          # username: ${GIT_USERNAME}
          # password: ${GIT_PASSWORD}
          clone-on-start: true
